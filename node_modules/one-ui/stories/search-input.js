import React from 'react';
import { storiesOf, action } from '@kadira/storybook';
import { SearchInput } from '../build/components';

module.exports = storiesOf('搜索框', module)
  .add('示例', () => (
    <div className="container">
      <SearchInput />
    </div>
  ));
