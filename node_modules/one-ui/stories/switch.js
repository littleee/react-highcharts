import React from 'react';
import { storiesOf, action } from '@kadira/storybook';
import { SwitchButton } from '../build/components';

module.exports = storiesOf('开关', module)
  .add('默认是否选中', () => (
    <div className="container">
      <div className="form-group">
        <label className="pure-u-1-6">默认选中：</label>
        <SwitchButton onChange={ action(`切换状态`) } />
      </div>
      <div className="form-group">
        <label className="pure-u-1-6">默认不选中：</label>
        <SwitchButton onChange={ action(`切换状态`) } checked />
      </div>
    </div>
  ));
