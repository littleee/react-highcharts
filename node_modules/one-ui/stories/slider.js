import React from 'react';
import { storiesOf, action } from '@kadira/storybook';
import { Slider } from '../build/components';

module.exports = storiesOf('滑块', module)
  .add('示例', () => (
    <div className="container">
      <Slider />
    </div>
  ));
