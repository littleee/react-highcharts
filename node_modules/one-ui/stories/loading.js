import React from 'react';
import { storiesOf, action } from '@kadira/storybook';
import { Loading } from '../build/components';

module.exports = storiesOf('加载器', module)
  .add('带文本', () => (
    <div className="container">
      <Loading children="拼命加载中..." />
    </div>
  ))
  .add('纯图标', () => (
    <div className="container">
      <Loading />
    </div>
  ));
