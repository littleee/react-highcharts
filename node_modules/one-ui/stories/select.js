import React from 'react';
import { storiesOf, action } from '@kadira/storybook';
import { Select } from '../build/components';

const options = [
  {
    label: 'hello',
    value: 'hello'
  },
  {
    label: 'world',
    value: 'world'
  }
];

module.exports = storiesOf('选择器', module)
  .add('示例', () => (
    <div className="container">
      <Select options={ options } multi placeholder="请输入关键词" />
    </div>
  ))
  .add('单选', () => (
    <div className="container">
      <Select options={ options } placeholder="请输入关键词" />
    </div>
  ))
  .add('多选', () => (
    <div className="container">
      <Select options={ options } multi placeholder="请输入关键词" />
    </div>
  ))
  .add('箭头居左', () => (
    <div className="container">
      <Select options={ options } multi placeholder="请输入关键词" />
    </div>
  ))
  .add('箭头居右', () => (
    <div className="container">
      <Select options={ options } multi pullRight placeholder="请输入关键词" />
    </div>
  ));
